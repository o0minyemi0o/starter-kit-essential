---
description: Storybook 스토리 작성 규칙. 컴포넌트 문서화 시 참고.
globs: "*.stories.jsx"
alwaysApply: false
---
# Storybook 작성 규칙

## 문서 스타일 원칙

Carbon Design System을 참고한 정제된 공식 문서 스타일을 따른다.

| 원칙 | 설명 |
|------|------|
| 미니멀 | 불필요한 장식, 이모지, 과한 강조 배제 |
| 전문적 | 공식 문서처럼 신뢰감 있는 톤 |
| 명확함 | 한 문장으로 핵심 전달 |
| 일관성 | 모든 컴포넌트 문서가 동일한 구조 |

### 금지 사항
- 🚫 이모지 과다 사용
- 🚫 "멋진", "강력한" 등 마케팅 문구
- 🚫 불필요한 색상 강조
- 🚫 장황한 설명

---

## 컴포넌트 설명 (docs.description)

디자이너가 이해할 수 있는 언어로 작성한다.

### 구조

```markdown
## 컴포넌트명

한 문장 정의. (이 컴포넌트가 무엇인지)

### 용도
- 언제 사용하는지 (When to use)
- 어떤 맥락에서 적합한지

### 주요 특징
- 특징 1
- 특징 2
```

### 예시 (Good)

```jsx
docs: {
  description: {
    component: `
## Button

사용자 인터랙션을 유도하는 기본 액션 요소입니다.

### 용도
- 폼 제출, 다이얼로그 확인 등 명확한 액션이 필요할 때
- 페이지 내 주요 행동을 강조할 때

### 주요 특징
- 3가지 강조 수준: Contained(강조) → Outlined(보조) → Text(약함)
- 6가지 색상 테마 지원
    `,
  },
},
```

### 예시 (Bad)

```jsx
// ❌ 너무 간략함
component: `버튼 컴포넌트입니다.`

// ❌ 개발자 용어 과다
component: `onClick 핸들러를 받아 이벤트를 트리거하는 컴포넌트`

// ❌ 마케팅 문구
component: `강력하고 유연한 버튼! 다양한 스타일을 지원합니다! ✨`
```

---

## argTypes 정의 규칙

### 필수 요소

```jsx
argTypes: {
  propName: {
    control: 'select',           // 적절한 control 타입
    options: ['a', 'b', 'c'],    // select/radio일 경우
    description: '한국어 설명',   // 디자이너 친화적 설명
    table: {
      type: { summary: 'string' },
      defaultValue: { summary: 'a' },
    },
  },
},
```

### control 타입 가이드

| props 타입 | control | 비고 |
|-----------|---------|------|
| 열거형 (3개 이하) | `radio` | 한눈에 보임 |
| 열거형 (4개 이상) | `select` | 드롭다운 |
| boolean | `boolean` | 토글 |
| string | `text` | 텍스트 입력 |
| number | `number` | 숫자 입력 |
| 색상 | `color` | 컬러 피커 |
| 객체/배열 | 정의하지 않음 | 문서 설명으로 대체 |

### description 작성

```jsx
// ✅ Good - 디자이너 관점
description: '버튼의 시각적 강조 수준을 결정합니다.'
description: '컴포넌트의 전체 너비 확장 여부를 지정합니다.'

// ❌ Bad - 개발자 용어
description: 'variant prop의 값'
description: 'boolean 타입의 fullWidth 속성'
```

---

## 스토리 구성 규칙

### 필수 스토리

| 스토리 | 목적 | 필수 |
|--------|------|------|
| Default | 가장 일반적인 사용 예시 | ✅ 필수 |

### 선택 스토리 (필요 시에만)

| 스토리 | 목적 | 포함 기준 |
|--------|------|----------|
| AllVariants | 주요 변형 한눈에 비교 | variant가 3개 이상일 때 |
| Sizes | 크기 변형 비교 | size prop 있을 때 |
| States | disabled, loading 등 | 상태 변형 있을 때 |

### 스토리 개수 제한

- **최소**: 1개 (Default)
- **권장**: 3-5개
- **최대**: 7개 (이상 시 분리 고려)

### 금지 사항

```jsx
// ❌ 과한 색상 나열
export const AllColors = {
  render: () => (
    <>
      <Button color="primary">Primary</Button>
      <Button color="secondary">Secondary</Button>
      <Button color="success">Success</Button>
      <Button color="error">Error</Button>
      <Button color="info">Info</Button>
      <Button color="warning">Warning</Button>
      // ... 12가지 색상 전부 나열
    </>
  ),
};

// ✅ 대표 색상만
export const Colors = {
  render: () => (
    <Stack direction="row" spacing={ 2 }>
      <Button color="primary">Primary</Button>
      <Button color="secondary">Secondary</Button>
      <Button color="error">Error</Button>
    </Stack>
  ),
};
```

---

## 색상/스타일링 제한

### 원칙

- 흑백 기반 + 브랜드 컬러(Primary)만 강조
- 무지개색 그라데이션 금지
- 배경색 사용 최소화

### 시연 스토리 스타일

```jsx
// ✅ 깔끔한 배치
render: () => (
  <Stack direction="row" spacing={ 2 }>
    <Button variant="contained">Contained</Button>
    <Button variant="outlined">Outlined</Button>
  </Stack>
),

// ❌ 과한 스타일링
render: () => (
  <Box sx={{ background: 'linear-gradient(...)', padding: 4 }}>
    <Button>Fancy Button</Button>
  </Box>
),
```

---

## 파일 구조 템플릿

```jsx
import Component from 'path/to/Component';
import Stack from '@mui/material/Stack';

export default {
  title: 'Component/ComponentName',
  component: Component,
  tags: ['autodocs'],
  parameters: {
    layout: 'centered',
    docs: {
      description: {
        component: `
## ComponentName

한 문장 정의.

### 용도
- 용도 1
- 용도 2

### 주요 특징
- 특징 1
- 특징 2
        `,
      },
    },
  },
  argTypes: {
    // props 정의
  },
};

/** 기본 사용 예시 */
export const Default = {
  args: {
    // 기본 args
  },
};

/** 변형 비교 (필요 시) */
export const AllVariants = {
  render: () => (
    <Stack direction="row" spacing={ 2 }>
      {/* 변형 나열 */}
    </Stack>
  ),
};
```

---

## 스토리 title 네이밍

| 섹션 | title 형식 | 예시 |
|------|-----------|------|
| Overview | `Overview/문서명` | `Overview/Introduction` |
| Style | `Style/토큰명` | `Style/Colors` |
| Component | `Component/컴포넌트명` | `Component/Button` |
| Template | `Template/템플릿명` | `Template/CardGrid` |
| Page | `Page/페이지명` | `Page/MainPage` |

---

## 체크리스트

스토리 작성 완료 전 확인:

- [ ] 한 문장 정의가 명확한가?
- [ ] 용도/특징이 디자이너 관점인가?
- [ ] argTypes에 description이 모두 있는가?
- [ ] 불필요한 스토리는 없는가? (3-5개 적정)
- [ ] 과한 색상/스타일링은 없는가?
- [ ] 이모지/마케팅 문구는 없는가?
